# CoreGREJS Environment Configuration

# JWT Secret (change in production!)
JWT_SECRET=coregre-secret-key-change-in-production

# Database (if using external MySQL)
# DATABASE_URL=mysql://user:password@host:3306/database
